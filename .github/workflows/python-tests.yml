name: Python API Tests

# Запускаем при пуше в main или ручном запуске
on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: # Позволяет запускать тесты кнопкой в интерфейсе

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10' # Укажите вашу версию

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f req.txt ]; then pip install -r req.txt; fi

    - name: Run tests
      env:
        # Прокидываем секреты в переменные окружения для тестов
        BASE_URL: ${{ secrets.BASE_URL }}
        TG_TOKEN: ${{ secrets.TG_TOKEN }}
        TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
      run: |
        # Запускаем pytest. Флаг -v для детальности, 
        # отчет в Телеграм уйдет через ваш хук pytest_terminal_summary
        pytest -v